<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:utilities="clr-namespace:Mathematics.App.Maui.UI.ViewModels.Utilities"
             xmlns:localizations="clr-namespace:Mathematics.App.Maui.Resources.Localizations"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             x:Class="Mathematics.App.Maui.UI.Views.Utilities.SoundGeneratorPage" x:DataType="utilities:SoundGeneratorViewModel">
	<ContentPage.Content>
		<ScrollView>
			<VerticalStackLayout Padding="20" Spacing="15">

				<Label Text="Тип волны" FontAttributes="Bold"/>
				<Picker ItemsSource="{Binding WaveTypes}" 
				        SelectedItem="{Binding SelectedWaveType}"/>

				<Label Text="Частота (Hz)" FontAttributes="Bold"/>
				<Slider Minimum="20" Maximum="20000" Value="{Binding Frequency}" />
				<Label Text="{Binding Frequency, StringFormat='{0:F0} Hz'}" HorizontalOptions="Center"/>
				
				<Label Text="Длительность (с)" FontAttributes="Bold"/>
				<Slider Minimum="1" Maximum="5" Value="{Binding Duration}" />
				<Label Text="{Binding Duration, StringFormat='{0:F0} с'}" HorizontalOptions="Center"/>
				
				<Label Text="Амплитуда" FontAttributes="Bold"/>
				<Slider Minimum="0" Maximum="10" Value="{Binding Amplitude}" />
				<Label Text="{Binding Amplitude, StringFormat='{0:F0}'}" HorizontalOptions="Center"/>
				
				<HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
					<Button Text="{x:Static localizations:Components_Resources.Button_Start}" Command="{Binding PlayCommand}" />
					<Button Text="{x:Static localizations:Components_Resources.Button_Stop}" Command="{Binding StopCommand}" />
				</HorizontalStackLayout>
				
				<lvc:CartesianChart Series="{Binding Series}" HeightRequest="400" ZoomMode="Both" />
				
				<Label Text="Сигнал (X / Y)" FontAttributes="Bold"/>
				<CollectionView ItemsSource="{Binding SignalValues}" HeightRequest="300">
					<CollectionView.ItemTemplate>
						<DataTemplate>
							<HorizontalStackLayout Padding="5">
								<Label Text="{Binding X, StringFormat='X: {0:F2}'}" WidthRequest="100"/>
								<Label Text="{Binding Y, StringFormat='Y: {0:F4}'}"/>
							</HorizontalStackLayout>
						</DataTemplate>
					</CollectionView.ItemTemplate>
				</CollectionView>
			</VerticalStackLayout>
		</ScrollView>
	</ContentPage.Content>
</ContentPage>